import Card from '../components/common/Card';

export default function HistoryPage() {
  return (
    <Card>
      <h2 className="text-2xl font-semibold text-slate-950">Learning History</h2>
      <p className="mt-2 text-slate-600">
        Historical results and weak-question analytics will be added later.
      </p>
    </Card>
  );
}
